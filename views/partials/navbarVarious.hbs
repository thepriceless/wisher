{{> navbarStart}}
        {{#if authorizedUser}}
            {{> navbarLoggedInUserImage }}
            {{> logoutButton}}
        {{else}}
            {{> loginButton}}
        {{/if}}
{{> navbarEnd}}